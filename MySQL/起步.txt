net start mysql80 - 启动
net stop mysql80 - 关闭

mysql -u root -p - MySQL服务登录
exit - MySQL退出

show databases - 展示所有表
use 表名
show 表名

select database(); - 展示目前在哪个库

为字段起别名
SELECT award_name as "award name" from awards;

去重(DISTINCT)
SELECT DISTINCT award_name from awards;

concat
拼接字段

IFNULL(Phone, 0)
如果Phone的值为null, 就把它变为0
