连接方面    
http1.0使用非持久连接，而http1.1使用持久连接，这样可以使多个http请求复用同一个TCP连接，避免使用非持久连接时每次需要建立连接的时间延迟

http1.1新增了host字段，用来指定服务器的域名

http1.1新增了put、head(获取报文首部，与GET相比，不返回报文主体部分)、options(询问支持的请求方法，用来跨域请求)方法

http1.1引入更多缓存策略，例如E-Tag