http2.0是一个彻底的二进制协议，头信息和数据体都是二进制

http2.0实现了多路复用，在一个连接里，客户端和服务器都可以同时发送多个请求或回应

http2.0实现了头信息压缩，由于http1.1协议不带状态，每次请求都必须附上所有信息，这样很多字段都是重复的，例如cookie，会浪费带宽和影响速度。http2.0会进行头信息压缩机制，所有字段存入一张表里，生成一个索引号，就不需要发送字段，转为发送索引号